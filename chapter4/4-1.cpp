#include "student_info.h"
#include <algorithm>
using namespace std;

int main() {
    //int maxlen = 0;
    string::size_type maxlen = 0;
    Student_info s;
    max(s.name.size(), maxlen);
    return 0;
}